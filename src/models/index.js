const ScannerModel = require('./scanner.model');
const ImageUploadModel = require('./imageUpload.model');
const ImageChunkModel = require('./imageChunk.model');
const ImageArtifactModel = require('./artifact.model');

module.exports = {
  ScannerModel,
  ImageUploadModel,
  ImageChunkModel,
  ImageArtifactModel,
};
